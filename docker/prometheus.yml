global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'karmayogi-app'
    static_configs:
      - targets: ['app:9090']
    scrape_interval: 30s
    metrics_path: '/metrics'

  - job_name: 'karmayogi-api'
    static_configs:
      - targets: ['app:3000']
    scrape_interval: 30s
    metrics_path: '/api/jobs/metrics'
    basic_auth:
      username: 'admin'
      password: 'change-this-password'